name: Build STM32 Project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Debug and Release
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/duvitech-llc/stm32-build-env:latest
      options: --user 1001

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # CMake and dependencies are assumed to be in the container image

    - name: Configure Debug Build
      run: |
        cmake -S . -B build/Debug -DCMAKE_BUILD_TYPE=Debug -DCMAKE_TOOLCHAIN_FILE=cmake/gcc-arm-none-eabi.cmake

    - name: Build Debug
      run: |
        cmake --build build/Debug

    - name: Configure Release Build
      run: |
        cmake -S . -B build/Release -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=cmake/gcc-arm-none-eabi.cmake

    - name: Build Release
      run: |
        cmake --build build/Release